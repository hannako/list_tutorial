%h1 My Todos

=form_for Todo.new, remote: true do |f|
  .form-group
    =f.text_field :description, placeholder: "what needs doing?"
    =f.text_field :priority, placeholder: "priority level"
    =f.submit

%ul
  = render @todos
//this renders _todo.html.haml it's rails magic
//the partial doesn't need @todos.each do |todo|
//rails automatically does that.
